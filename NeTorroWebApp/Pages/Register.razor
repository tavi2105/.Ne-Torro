@page "/Register"
@using Models 
<h1>Register</h1>
<EditForm Model="@user" OnValidSubmit="@OnValidSubmit" class ="section-1-container section-container">
    <DataAnnotationsValidator />
    <div class="form-group">
        <label class="label-text">First Name :</label>
        <div>
            <InputText @bind-Value="@user.FirstName" class="form-control col-sm-5" />
            <ValidationMessage For="@(() => user.FirstName)" />
        </div>
    </div>
    <div class="form-group">
        <label class="label-text">Last Name :</label>
        <div>
            <InputText @bind-Value="@user.LastName" class="form-control col-sm-5" />
            <ValidationMessage For="@(() => user.LastName)" />
        </div>
    </div>
    <div class="form-group">
        <label class="label-text"> Phone number:</label>
        <div>
            <InputText @bind-Value="@user.PhoneNumber" class="form-control col-sm-5" />
            <ValidationMessage For="@(() => user.PhoneNumber)" />
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label class="label-text">Birthday</label>
            <div>
                <InputDate @bind-Value="@user.Birthday" class="form-control col-sm-5" />
                <ValidationMessage For="@(() => user.Birthday)" />
            </div>
        </div>
    </div>
    <button id="register-btn" type="submit" class="btn btn-primary">
        @ButtonText
    </button>
</EditForm>

@code {

    public User user = new User();

    public string ButtonText { get; set; } = "Register";

    public EventCallback OnValidSubmit { get; set; }



}


<style>
    h1 {
        color: navajowhite;
        font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
        font-size:100px;
        align-self:center;
        margin-top: 0px;
    }
    .register-body {
        display: flex;
        align-content: center;
        justify-content: center;
        align-items: center;
        margin: 0px;
    }
    .label-text {
        font-size: 30px;
        color: lightgoldenrodyellow
    }
    .form-control {
        width: 300px;
        height: 40px;
        border-radius: 25px;
    }

    #register-btn {
        display: inline-block;
        border-radius: 4px;
        background-color: #f4511e;
        border: none;
        color: #FFFFFF;
        text-align: center;
        font-size: 28px;
        margin-top: 30px;
        width: 200px;
        height: 45px;
        transition: all 0.5s;
        cursor: pointer;
        margin-right: 30px;
    }

        #register-btn span {
            cursor: pointer;
            display: inline-block;
            position: relative;
            transition: 0.5s;
        }

            #register-btn span:after {
                content: '\00bb';
                position: absolute;
                opacity: 0;
                top: 0;
                right: -20px;
                transition: 0.5s;
            }

        #register-btn:hover span {
            padding-right: 25px;
        }

            #register-btn:hover span:after {
                opacity: 1;
                right: 0;
            }
</style>